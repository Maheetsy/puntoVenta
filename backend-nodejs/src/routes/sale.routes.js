// routes/sale.routes.js
// Este archivo se implementará en el Sprint 3
const express = require('express');
const router = express.Router();

// Placeholder para Sprint 3
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de ventas - Pendiente Sprint 3' });
});

module.exports = router;